name: Generate RSS Feeds

on:
  push:
    paths:
      - 'forge-source/static/data/flags.json'
      - 'forge-source/static/pie_brief.json'
      - 'forge-source/static/pie_brief_history.json'
      - 'generate_flags_rss.py'
      - 'generate_brief_rss.py'
  workflow_dispatch:

jobs:
  rss:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate flags.xml
        run: python3 generate_flags_rss.py

      - name: Generate brief.xml
        run: python3 generate_brief_rss.py

      - name: Commit RSS feeds
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "forge-source/static/flags.xml"
          git add "forge-source/static/brief.xml"
          git diff --cached --quiet && echo "No changes" || \
            git commit -m "chore: regenerate RSS feeds [skip ci]"
          git push
