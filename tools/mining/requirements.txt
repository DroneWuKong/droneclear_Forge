requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# curl_cffi impersonates browser TLS fingerprints (JA3/JA4), bypassing Cloudflare
# bot detection that blocks Python requests. Optional — falls back to requests.
curl-cffi>=0.6.0
