/*
 * dash-brief.js — intentionally retired.
 *
 * The generated "At a glance" card duplicated information now presented by the
 * native Patterns hub, current-signal surfaces, Priority View, and Ask PIE.
 * Keeping this path as a no-op preserves compatibility with older generated HTML
 * and immutable asset URLs while removing the redundant visual card immediately.
 *
 * The global point-of-reliance disclaimer remains injected at the bottom of the
 * relevant pages by build_static.py.
 */
(() => {
  'use strict';
})();
